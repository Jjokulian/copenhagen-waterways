#!/usr/bin/env python3
"""docs/hypodrafts/L3.md - generated from the hand-written draft as committed at c601feb.

The prose is the draft's, moved here verbatim and then made checkable: counts of
files held in this repository read live from data/derived/drafts_b_counts.json;
counts the drafting session made from external sources or one-off runs carried as
quotations of that commit (located references, {was:COMMIT:FILE:phrase with @@}); register IDs as {ref:}; chemical species
as {chem:}. See scripts/pages/drafts_b_page.py and LIVE_NUMBERS.md.

    python3 scripts/pages/hypodraft_l3.py
"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from drafts_b_page import render

TEXT = r'''# {ref:L3|title}

*Generated by `scripts/pages/hypodraft_l3.py` from the draft as committed at `c601feb`. Counts of files held in this repository are read live. Counts that session made from external sources or from one-off runs are quotations of that commit - what the draft reported, shown as such, not re-derived by the build.*


*Test draft. Status: procedure specified and run once on bed oxygen; numbers below are that run, not a literature quote.*

## The observable consequence, stated so it can fail

{ref:L3} has a weak reading and a strong reading, and only the strong one is a claim about the sea.

**Weak:** *a slope fitted to a Danish series changes when you move the cut.* This is true of every noisy series ever fitted and is not worth testing — see the null below, which produces it {was:c601feb:docs/hypodrafts/L3.md:which produces it @@% of the}% of the time from nothing.

**Strong:** *the window-dependence exceeds what a single constant rate plus the series' own autocorrelated noise would produce* — i.e. the record contains genuinely different eras, so "the trend" is not a well-defined quantity and reporting one number is a category error.

**Falsified if:** over the full space of admissible windows, the spread of fitted slopes, and the fraction of them that are positive, both fall inside the interval generated by simulating one constant slope with matched `AR(1)` noise. Then the window-dependence is noise around a single rate, the number is estimable, and {ref:L3}'s strong reading is not established for this variable.

**Supported if:** the observed surface is more dispersed, or its sign-share further from a single-trend expectation, than that simulation allows.

## Data, counted

`docs/data/areas/stations_series.{json,bin}` — {fig:dbx_ss_total} station-months (verified: `sum(n)` over the {fig:dbx_ss_nvars} variables; `stations_series.bin` is {fig:dbx_ss_bytes} bytes, exactly the declared layout), {fig:dbx_ss_stations} stations, month index {was:c601feb:docs/hypodrafts/L3.md:stations, month index @@–560 = 1980-01}–{was:c601feb:docs/hypodrafts/L3.md:stations, month index 0–@@ = 1980-01 to 2026-09.} = 1980-{was:c601feb:docs/hypodrafts/L3.md:index 0–560 = 1980-@@ to 2026-09. Variable used:} to 2026-{was:c601feb:docs/hypodrafts/L3.md:= 1980-01 to 2026-@@. Variable used: **`oxy_bed`**,}.

Variable used: **`oxy_bed`**, near-bed oxygen in mg/l, **{fig:dbx_ss_oxy_bed}** rows. Dropped **{was:c601feb:docs/hypodrafts/L3.md:rows. Dropped **@@** as impossible}** as impossible ({was:c601feb:docs/hypodrafts/L3.md:**472** as impossible (@@ at ≤ 0 mg/l,} at ≤ {was:c601feb:docs/hypodrafts/L3.md:(432 at ≤ @@ mg/l, 40 above} mg/l, {was:c601feb:docs/hypodrafts/L3.md:≤ 0 mg/l, @@ above 20 mg/l;} above {was:c601feb:docs/hypodrafts/L3.md:mg/l, 40 above @@ mg/l; the raw} mg/l; the raw range is −{was:c601feb:docs/hypodrafts/L3.md:raw range is −@@ to 281.6) — **error} to {was:c601feb:docs/hypodrafts/L3.md:range is −9.93 to @@) — **error class}) — **error class 1, value error**. Kept **{was:c601feb:docs/hypodrafts/L3.md:error**. Kept **@@** rows on}** rows on **{was:c601feb:docs/hypodrafts/L3.md:rows on **@@** stations. Two}** stations.

Two further error classes are structural and are not fixed, only declared. The archive is already a **monthly median per station** — **class 3, aggregation**: within-month spread and the diel oxygen cycle are gone before I see the file, and there is **no time-of-day column anywhere** (**class 6, absent dimension**). Both inflate the year-to-year residual; the null below is matched to the residual actually observed, so they widen the null rather than being smuggled into the signal. The saturation cross-check uses `oxysat_bed`, which is computed from oxygen, temperature and salinity — **class 7, model-as-datum** — and is reported as a robustness check only.

## The constraint, and therefore the null

Constraint imposed: annual index years **1989–2025** ({was:c601feb:docs/hypodrafts/L3.md:years **1989–2025** (@@ years; before 1989} years; before 1989 fewer than {was:c601feb:docs/hypodrafts/L3.md:fewer than @@ stations report} stations report per year, 2026 is a part-year of {was:c601feb:docs/hypodrafts/L3.md:part-year of @@ rows), windows} rows), windows of **≥ {was:c601feb:docs/hypodrafts/L3.md:windows of **≥ @@ years**, giving} years**, giving **{was:c601feb:docs/hypodrafts/L3.md:giving **@@ admissible windows**.} admissible windows**. Index = two-way fixed effects (station + calendar month, alternating demeaning to convergence), then the annual mean of residuals. Station effects absorb level differences, so composition change in *levels* cannot drive the index.

**The null is not "zero sensitivity".** For each station set I estimate ρ and σ from the detrended index, then simulate {was:c601feb:docs/hypodrafts/L3.md:then simulate @@ AR(1) series} `AR(1)` series of the same length and compute the same {was:c601feb:docs/hypodrafts/L3.md:compute the same @@-window surface. Two}-window surface. Two nulls:

- **`H0`, no trend at all.** Sign-flipping windows occur in **{was:c601feb:docs/hypodrafts/L3.md:windows occur in **@@%** of simulations, in}%** of simulations, in every station set. Median slope range {was:c601feb:docs/hypodrafts/L3.md:slope range @@ mg/l/yr (pooled).} mg/l/yr (pooled). *A sign flip carries no information whatsoever.*
- **`H1`, one constant slope equal to the fitted full-window slope, plus matched noise.** This is the null that {ref:L3}'s strong reading must beat.

## Result of the run

| set | stations | full-window slope | ρ | obs. slope range | frac. positive | P(`H0` frac ≤ obs) | P(`H1` frac ≤ obs) | P(`H1` range ≥ obs) |
|---|---|---|---|---|---|---|---|---|
| pooled | {was:c601feb:docs/hypodrafts/L3.md:| pooled | @@ | −0.0064 |} | −{was:c601feb:docs/hypodrafts/L3.md:| 1,325 | −@@ | +0.35 | 0.112} | +{was:c601feb:docs/hypodrafts/L3.md:| −0.0064 | +@@ | 0.112 | 0.145} | {was:c601feb:docs/hypodrafts/L3.md:| +0.35 | @@ | 0.145 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.112 | @@ | 0.023 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.145 | @@ | 0.150 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.023 | @@ | 0.360 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.150 | @@ | | core} |
| core (≥{was:c601feb:docs/hypodrafts/L3.md:| | core (≥@@ yrs) | 186 |} yrs) | {was:c601feb:docs/hypodrafts/L3.md:(≥20 yrs) | @@ | −0.0085 |} | −{was:c601feb:docs/hypodrafts/L3.md:| 186 | −@@ | +0.44 | 0.138} | +{was:c601feb:docs/hypodrafts/L3.md:| −0.0085 | +@@ | 0.138 | 0.108} | {was:c601feb:docs/hypodrafts/L3.md:| +0.44 | @@ | 0.108 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.138 | @@ | 0.008 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.108 | @@ | 0.091 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.008 | @@ | 0.358 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.091 | @@ | | year-round} |
| year-round (R<{was:c601feb:docs/hypodrafts/L3.md:| | year-round (R<@@) | 427 |}) | {was:c601feb:docs/hypodrafts/L3.md:year-round (R<0.2) | @@ | −0.0052 |} | −{was:c601feb:docs/hypodrafts/L3.md:| 427 | −@@ | +0.50 | 0.105} | +{was:c601feb:docs/hypodrafts/L3.md:| −0.0052 | +@@ | 0.105 | 0.170} | {was:c601feb:docs/hypodrafts/L3.md:| +0.50 | @@ | 0.170 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.105 | @@ | 0.049 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.170 | @@ | 0.177 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.049 | @@ | 0.436 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.177 | @@ | | summer-peaked} |
| summer-peaked (R>{was:c601feb:docs/hypodrafts/L3.md:| | summer-peaked (R>@@) | 426 |}) | {was:c601feb:docs/hypodrafts/L3.md:summer-peaked (R>0.5) | @@ | −0.0035 |} | −{was:c601feb:docs/hypodrafts/L3.md:| 426 | −@@ | −0.15 | 0.171} | −{was:c601feb:docs/hypodrafts/L3.md:| −0.0035 | −@@ | 0.171 | 0.352} | {was:c601feb:docs/hypodrafts/L3.md:| −0.15 | @@ | 0.352 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.171 | @@ | 0.246 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.352 | @@ | 0.448 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.246 | @@ | 0.429 |} | {was:c601feb:docs/hypodrafts/L3.md:| 0.448 | @@ | Slopes in} |

Slopes in mg/l/yr. Fitted slopes run from **−{was:c601feb:docs/hypodrafts/L3.md:run from **−@@ to +0.084** (core} to +{was:c601feb:docs/hypodrafts/L3.md:from **−0.054 to +@@** (core set) —}** (core set) — a {was:c601feb:docs/hypodrafts/L3.md:set) — a @@-fold range crossing}-fold range crossing zero. With the end fixed at 2025 every start year gives a negative slope (−{was:c601feb:docs/hypodrafts/L3.md:a negative slope (−@@ at 1989, −0.054 at} at 1989, −{was:c601feb:docs/hypodrafts/L3.md:(−0.0085 at 1989, −@@ at 2016); with the} at 2016); with the start fixed at 1989 the slope is **+{was:c601feb:docs/hypodrafts/L3.md:the slope is **+@@** ending 1998 and}** ending 1998 and **−{was:c601feb:docs/hypodrafts/L3.md:ending 1998 and **−@@** ending 2025. **The}** ending 2025. **The sign is set by the end year, not the start year.**

**Every dispersion statistic is consistent with `H1`.** P(`H1` range ≥ observed) is {was:c601feb:docs/hypodrafts/L3.md:≥ observed) is @@–0.44 everywhere: one}–{was:c601feb:docs/hypodrafts/L3.md:observed) is 0.36–@@ everywhere: one constant} everywhere: one constant rate plus this series' own autocorrelation reproduces the whole surface.

## The two confounds

**Composition.** The summer-peaked share of `oxy_bed` station-months falls from {was:c601feb:docs/hypodrafts/L3.md:station-months falls from @@ (1989–94) to 0.035} (1989–1994) to {was:c601feb:docs/hypodrafts/L3.md:0.126 (1989–94) to @@ (2015–19) — the} (2015–2019) — the compositional shift is real and large in this variable. It is handled by re-running on a fixed {was:c601feb:docs/hypodrafts/L3.md:a fixed @@-station core}-station core panel and on the {was:c601feb:docs/hypodrafts/L3.md:on the @@ year-round stations} year-round stations separately; both give the same answer, and the core panel gives a *stronger* directional signal, not a weaker one. My Rayleigh classification ({was:c601feb:docs/hypodrafts/L3.md:Rayleigh classification (@@ summer /} summer / {was:c601feb:docs/hypodrafts/L3.md:summer / @@ year-round) is} year-round) is computed on `oxy_bed` rows only and does not reproduce the project's {was:c601feb:docs/hypodrafts/L3.md:reproduce the project's @@/481 — treat} / {was:c601feb:docs/hypodrafts/L3.md:the project's 293/@@ — treat the} — treat the split as re-derived, not inherited.

**Thermometer.** Repeating on `oxysat_bed` ({fig:dbx_ss_oxysat_bed} rows, {was:c601feb:docs/hypodrafts/L3.md:rows, @@ dropped): the} dropped): the fraction of positive windows rises from {was:c601feb:docs/hypodrafts/L3.md:windows rises from @@–0.15 to **0.28–0.30**,}–{was:c601feb:docs/hypodrafts/L3.md:rises from 0.11–@@ to **0.28–0.30**, and} to **{was:c601feb:docs/hypodrafts/L3.md:from 0.11–0.15 to **@@–0.30**, and the full-window}–{was:c601feb:docs/hypodrafts/L3.md:from 0.11–0.15 to **0.28–@@**, and the full-window}**, and the full-window slope is −{was:c601feb:docs/hypodrafts/L3.md:slope is −@@ %sat/yr. Moving from} %sat/yr. Moving from a concentration to a saturation ruler roughly doubles the sign ambiguity. Consistent with part of the mg/l decline being solubility, not oxygen supply. Not separated here.

## What this licenses

Licensed: for near-bed oxygen in this archive, **{ref:L3}'s strong reading is not established**. The window-dependence is fully reproduced by one constant slope with matched autocorrelation, and the interesting asymmetry is the opposite of {ref:L3}'s framing — start year barely matters, end year decides the sign.

Not licensed: nothing about chlorophyll, nutrient loading, or the modelled reference condition, which is where {ref:L3}'s stake actually lies ({ref:L1}, {ref:L2}). Nothing about pre-1980, which is the era {ref:L3} is really about — this archive starts at 1980, after the loading peak, so it cannot test "a trend measured from a peak". Nothing causal. And a negative here does not license reporting a single slope: the fitted rate still ranges −{was:c601feb:docs/hypodrafts/L3.md:rate still ranges −@@ to +0.084, so any} to +{was:c601feb:docs/hypodrafts/L3.md:ranges −0.054 to +@@, so any published}, so any published number needs its window and its `H1` interval printed beside it.
'''

if __name__ == "__main__":
    sys.exit(render("docs/hypodrafts/L3.md", TEXT))
