#!/usr/bin/env python3
"""docs/hypodrafts/G1.md - generated from the hand-written draft as committed at c601feb.

The prose is the draft's, moved here verbatim and then made checkable: counts of
files held in this repository read live from data/derived/drafts_b_counts.json;
counts the drafting session made from external sources or one-off runs carried as
quotations of that commit (located references, {was:COMMIT:FILE:phrase with @@}); register IDs as {ref:}; chemical species
as {chem:}. See scripts/pages/drafts_b_page.py and LIVE_NUMBERS.md.

    python3 scripts/pages/hypodraft_g1.py
"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from drafts_b_page import render

TEXT = r'''# {ref:G1|title}

*Generated by `scripts/pages/hypodraft_g1.py` from the draft as committed at `c601feb`. Counts of files held in this repository are read live. Counts that session made from external sources or from one-off runs are quotations of that commit - what the draft reported, shown as such, not re-derived by the build.*


Draft only. Counts verified by reading the files; the pilot slopes below **carry no null**,
so none is a result.

## The claim, and why it needs splitting before it can be tested

{ref:G1} predicts "monotone worsening independent of load", and offers that as why load reduction
has not produced recovery. But warming reaches near-bed oxygen by three routes —
**solubility** (deterministic, Weiss 1970), **stratification** ({ref:C1}'s object) and **metabolic
rate** (Q10, unmeasured here) — only the first of which is policy-inert. So {ref:G1}'s testable
content is not *does oxygen fall* but **what fraction of the long-term fall is
thermodynamics**: the seasonal decomposition done here ({was:c601feb:docs/hypodrafts/G1.md:done here (@@-fold →}-fold → {was:c601feb:docs/hypodrafts/G1.md:here (23-fold → @@-fold on a}-fold on a saturation
threshold, ~{was:c601feb:docs/hypodrafts/G1.md:saturation threshold, ~@@ % thermodynamic), moved} % thermodynamic), moved onto the long-term axis.

## Observable consequence, stated to be falsified

Per station-month, `C_sat` = Weiss (1970) from near-bed T and S, mL/L × {was:c601feb:docs/hypodrafts/G1.md:and S, mL/L × @@: > `O_bed =}:

> `O_bed = C_sat(T_bed, S_bed) − D`,  so  `dO/dt ≡ dC_sat/dt − dD/dt`.

`C_sat` is exact and external, so the first term is **computed, not estimated**. The deficit
`D` is solubility-neutral by construction and carries the rest: consumption, failed
ventilation, advection.

**G1-a:** *the decline is not mostly solubility* — `dC_sat/dt` is a minority of `dO/dt`.
Falsified if the solubility term equals or exceeds the observed decline.

**G1-b:** *`dD/dt > 0`* — the deficit widens, as it must if warming drives respiration or
stratification faster than ventilation recovers. Falsified by `dD/dt ≤ 0`, or by a `dD/dt`
indistinguishable from the year-block null. G1-b carries the load-independence claim; G1-a
decides whom the finding is about.

## Data, with verified counts

`docs/data/areas/stations_series.{json,bin}` — {fig:dbx_ss_total} station-months, {fig:dbx_ss_stations} stations, months
{was:c601feb:docs/hypodrafts/G1.md:stations, months @@–560 (Jan}–{was:c601feb:docs/hypodrafts/G1.md:stations, months 0–@@ (Jan 1980 –} (Jan 1980 – Sep 2026). Five-way join `oxy_bed ∩ temp_bed ∩ sal_bed ∩ temp_surf ∩
sal_surf`: **{was:c601feb:docs/hypodrafts/G1.md:sal_surf`: **@@**; after}**; after plausibility ({was:c601feb:docs/hypodrafts/G1.md:**76,309**; after plausibility (@@ ≤ O ≤ 20,} ≤ O ≤ {was:c601feb:docs/hypodrafts/G1.md:(0 ≤ O ≤ @@, −2 ≤ T}, −{was:c601feb:docs/hypodrafts/G1.md:O ≤ 20, −@@ ≤ T ≤ 30,} ≤ T ≤ {was:c601feb:docs/hypodrafts/G1.md:−2 ≤ T ≤ @@, 0 ≤ S}, {was:c601feb:docs/hypodrafts/G1.md:T ≤ 30, @@ ≤ S ≤} ≤ S ≤ {was:c601feb:docs/hypodrafts/G1.md:≤ S ≤ @@ at both depths)} at both
depths) **{was:c601feb:docs/hypodrafts/G1.md:both depths) **@@ rows, 1,314 stations,} rows, {was:c601feb:docs/hypodrafts/G1.md:depths) **76,264 rows, @@ stations, 1980–2026**. Trend} stations, 1980–2026**.

Trend panel = cells (station × calendar month) with ≥ {was:c601feb:docs/hypodrafts/G1.md:with ≥ @@ distinct years} distinct years **and** ≥ {was:c601feb:docs/hypodrafts/G1.md:**and** ≥ @@-year span:}-year
span: **{was:c601feb:docs/hypodrafts/G1.md:15-year span: **@@ cells · 54,211} cells · {was:c601feb:docs/hypodrafts/G1.md:**2,429 cells · @@ rows · 310} rows · {was:c601feb:docs/hypodrafts/G1.md:rows · @@ stations**. Balanced} stations**. Balanced variant (cell in both 1990–1999
and 2015–2026): {was:c601feb:docs/hypodrafts/G1.md:1990–99 and 2015–26): @@ cells · 35,215} cells · {was:c601feb:docs/hypodrafts/G1.md:cells · @@ rows. Rows} rows. Rows per half-decade fall {was:c601feb:docs/hypodrafts/G1.md:half-decade fall @@ (1990–94) →} (1990–1994) → {was:c601feb:docs/hypodrafts/G1.md:14,491 (1990–94) → @@ (2020–24): the network}
(2020–2024): the network thins, which is the composition problem.

**Error classes.** class 1, value — `oxy_bed` −{was:c601feb:docs/hypodrafts/G1.md:value — `oxy_bed` −@@ to 281.6 mg/l, `temp_surf`} to {was:c601feb:docs/hypodrafts/G1.md:`oxy_bed` −9.93 to @@ mg/l, `temp_surf` to} mg/l, `temp_surf` to {was:c601feb:docs/hypodrafts/G1.md:mg/l, `temp_surf` to @@ °C; 45 rows} °C; {was:c601feb:docs/hypodrafts/G1.md:to 80.3 °C; @@ rows removed. (2)} rows
removed. class 2, quantisation — one-decimal T; ±{was:c601feb:docs/hypodrafts/G1.md:— one-decimal T; ±@@ °C is ±0.015 mg/l} °C is ±{was:c601feb:docs/hypodrafts/G1.md:°C is ±@@ mg/l in `C_sat`, which} mg/l in `C_sat`, which
matters for a single value, not a slope. class 3, aggregation — monthly medians, so
`D` may pair T, S and O from different casts. class 5, unfilled — {was:c601feb:docs/hypodrafts/G1.md:unfilled — @@ `oxy_bed` cells} `oxy_bed` cells carry no
archived saturation. class 6, **no time-of-day column anywhere** (`ctd.csv.gz` has `Dato` as
`19701013`); if sampling hour drifted over {was:c601feb:docs/hypodrafts/G1.md:drifted over @@ years, the} years, the diel oxygen cycle drifted with it,
uncorrectably — the one class that caps this test rather than bounds it. class 7, avoided: the
unit is a station, and no water body is needed, every estimate being within-cell.

## Circularity, avoided

Archived `oxysat_bed` is computed from O, T and S, so it already contains temperature.
**It is never an outcome here.** `D` contains T through `C_sat` deliberately — that is the
*subtraction* of the solubility channel, Weiss being external to the archive; and errors in
T enter `D` but not the year regressor, so they add noise, not trend. Worked through in
[`C6.md`](C6.md); not restated.

## The null, computed under the constraint actually imposed

Not "zero slope". The estimator is a within-cell FE slope on year; the constraint imposed is
*within station and within calendar month*. Under it I measured the dependence the textbook
SE ignores:

- Autocorrelation of within-cell detrended `D` residuals at annual spacing: first-lag mean
  **−{was:c601feb:docs/hypodrafts/G1.md:lag-1 mean **−@@**, lag-2 **−0.057**}**, second-lag **−{was:c601feb:docs/hypodrafts/G1.md:mean **−0.048**, lag-2 **−@@** over 2,312 cells}** over {was:c601feb:docs/hypodrafts/G1.md:lag-2 **−0.057** over @@ cells — indistinguishable} cells — indistinguishable from detrending's own
  −{was:c601feb:docs/hypodrafts/G1.md:from detrending's own −@@/(n−1) ≈ −0.05 bias.}/(n−{was:c601feb:docs/hypodrafts/G1.md:from detrending's own −1/(n−@@) ≈ −0.05 bias.}) ≈ −{was:c601feb:docs/hypodrafts/G1.md:own −1/(n−1) ≈ −@@ bias. **Temporal AR is} bias. **Temporal AR is not the problem here.**
- The dependence is *cross-sectional*: SD of the year-mean residual **{was:c601feb:docs/hypodrafts/G1.md:year-mean residual **@@** where independence}** where
  independence predicts **{was:c601feb:docs/hypodrafts/G1.md:independence predicts **@@** — variance}** — variance inflation **{was:c601feb:docs/hypodrafts/G1.md:variance inflation **@@**. All cells}**. All cells in a year sample the same weather.

So the null is a **year-block procedure**: resample the {was:c601feb:docs/hypodrafts/G1.md:resample the @@ calendar years} calendar years with replacement,
and separately permute year labels attached to whole years; refit, {was:c601feb:docs/hypodrafts/G1.md:years; refit, @@ draws. That} draws. That keeps
every station effect, the seasonal cycle and the within-year common shock, destroying only
the year→outcome ordering. Consequence: the naive SE on `dD/dt` ({was:c601feb:docs/hypodrafts/G1.md:SE on `dD/dt` (@@ mg/l/yr) understates by ≈} mg/l/yr)
understates by ≈ √{was:c601feb:docs/hypodrafts/G1.md:understates by ≈ √@@ ≈ **5.3×**, to ≈} ≈ **{was:c601feb:docs/hypodrafts/G1.md:≈ √28.4 ≈ **@@×**, to ≈ 0.0032.}×**, to ≈ {was:c601feb:docs/hypodrafts/G1.md:≈ **5.3×**, to ≈ @@. A quoted null}. A quoted null would have been wrong by that
factor.

## Composition, handled

The phenology mix moves. On my own definition (≥ {was:c601feb:docs/hypodrafts/G1.md:definition (≥ @@ `oxy_bed` months;} `oxy_bed` months; ≥ {was:c601feb:docs/hypodrafts/G1.md:`oxy_bed` months; ≥ @@ % in May–Oct} % in May–Oct →
"summer-peaked"): **{was:c601feb:docs/hypodrafts/G1.md:"summer-peaked"): **@@ summer-peaked vs} summer-peaked vs {was:c601feb:docs/hypodrafts/G1.md:summer-peaked vs @@ year-round**, the} year-round**, the summer-peaked running
**{was:c601feb:docs/hypodrafts/G1.md:summer-peaked running **@@ mg/l lower** in} mg/l lower** in May–Oct ({was:c601feb:docs/hypodrafts/G1.md:lower** in May–Oct (@@ vs 7.56, d **0.50**,} vs {was:c601feb:docs/hypodrafts/G1.md:in May–Oct (6.31 vs @@, d **0.50**, n}, d **{was:c601feb:docs/hypodrafts/G1.md:vs 7.56, d **@@**, n = 12,390}**, n = {was:c601feb:docs/hypodrafts/G1.md:**0.50**, n = @@ / 32,924), their} / {was:c601feb:docs/hypodrafts/G1.md:n = 12,390 / @@), their share of}), their share
of station-months moving **{was:c601feb:docs/hypodrafts/G1.md:station-months moving **@@ % (2010–14) →} % (2010–2014) → {was:c601feb:docs/hypodrafts/G1.md:% (2010–14) → @@ % (2015–19)**. Direction} % (2015–2019)**. Direction and size reproduce the figure
on file ({was:c601feb:docs/hypodrafts/G1.md:figure on file (@@/481, 1.57 mg/l, d} / {was:c601feb:docs/hypodrafts/G1.md:figure on file (293/@@, 1.57 mg/l, d}, {was:c601feb:docs/hypodrafts/G1.md:on file (293/481, @@ mg/l, d 0.66,} mg/l, d {was:c601feb:docs/hypodrafts/G1.md:(293/481, 1.57 mg/l, d @@, 32→12 %); the}, {was:c601feb:docs/hypodrafts/G1.md:1.57 mg/l, d 0.66, @@→12 %); the split}→{was:c601feb:docs/hypodrafts/G1.md:mg/l, d 0.66, 32→@@ %); the split is} %); the split is threshold-dependent, which is
itself the warning.

Three defences, all required: (i) within-cell FE, so a station only competes with itself;
(ii) the balanced panel, so entry and exit cannot tilt the slope; (iii) phenology × year.

## Procedure

1. Read the five arrays at the `.json` offsets, key `station×1000 + month`, filter → {was:c601feb:docs/hypodrafts/G1.md:month`, filter → @@; compute `C_sat`};
   compute `C_sat` (Weiss) and `D`; build the ≥ {was:c601feb:docs/hypodrafts/G1.md:build the ≥ @@-year / ≥}-year / ≥ {was:c601feb:docs/hypodrafts/G1.md:8-year / ≥ @@-span panel. 2.}-span panel.
2. FE slopes on year for `O`, `C_sat`, `D`, `T_bed`, `T_surf − T_bed`. The first three satisfy
   the identity exactly; report the split as a **fraction of `dO/dt`**, not a p-value.
3. Year-block null (above) for each; report naive SE, block SE and their ratio.
4. Repeat balanced, Jun–Sep, Dec–Mar, and with the phenology interaction. **Agreement across
   all five is the finding; disagreement is equally the finding.**
5. Refit `dD/dt` with surface-minus-bed density as a covariate — this *uses* {ref:C1}'s variable and
   does not test it. What survives is the metabolic-plus-unknown residue, a residue not a Q10.

### What the pilot already shows (no null attached — not yet a result)

On the {was:c601feb:docs/hypodrafts/G1.md:On the @@-cell panel,}-cell panel, per decade: `T_bed` **+{was:c601feb:docs/hypodrafts/G1.md:decade: `T_bed` **+@@ °C**, `C_sat`} °C**, `C_sat` **−{was:c601feb:docs/hypodrafts/G1.md:**+0.42 °C**, `C_sat` **−@@ mg/l**, `O_bed` **−0.046 mg/l**,} mg/l**, `O_bed`
**−{was:c601feb:docs/hypodrafts/G1.md:**−0.099 mg/l**, `O_bed` **−@@ mg/l**, `D` **−0.053 mg/l**.} mg/l**, `D` **−{was:c601feb:docs/hypodrafts/G1.md:mg/l**, `D` **−@@ mg/l**. The solubility term} mg/l**. The solubility term exceeds the whole observed
decline; the deficit appears to *narrow*. But `dD/dt` sits ~{was:c601feb:docs/hypodrafts/G1.md:sits ~@@ block-SE from} block-SE from zero and moves
to −{was:c601feb:docs/hypodrafts/G1.md:moves to −@@ (balanced), −0.088 (Jun–Sep),} (balanced), −{was:c601feb:docs/hypodrafts/G1.md:(balanced), −@@ (Jun–Sep),} (Jun–Sep), −{was:c601feb:docs/hypodrafts/G1.md:(Jun–Sep), −@@ (Dec–Mar) —} (Dec–Mar) — spread wider than its own
uncertainty. Surface-minus-bed temperature difference **−{was:c601feb:docs/hypodrafts/G1.md:temperature difference **−@@ °C/decade**: no detectable} °C/decade**: no detectable
strengthening of thermal stratification here.

## What a result would and would not license

**Would.** If `dC_sat/dt` matches or exceeds `dO/dt` under the year-block null and across all
five sensitivities, that licenses: *at these {was:c601feb:docs/hypodrafts/G1.md:*at these @@ stations the} stations the long-term near-bed oxygen
decline is accounted for by the solubility ceiling alone, and the non-thermodynamic deficit
did not widen.* That constrains {ref:G1}'s headline directly: "monotone worsening independent of
load" would be true of the water and still not evidence that load reduction failed, the
falling term being the one no load policy touches.

**Would not.** It does not measure respiration and cannot: `D` is a standing stock, Q10 a
rate, and converting between them needs a ventilation timescale the archive lacks (class 6).
A narrowing `D` is equally consistent with load reduction working *and* with the network
drifting toward better-ventilated positions — (i)–(iii) bound that, they do not eliminate
it. It says nothing about stratification beyond a two-point difference at sampling depth, which
is no pycnocline ({ref:C1}'s object; believe {ref:C1} here), nothing about water bodies, nothing about
the {was:c601feb:docs/hypodrafts/G1.md:about the @@ stations the} stations the span filter drops. And `dD/dt ≈ 0` refutes G1-b only at the
resolution of monthly medians taken at an unrecorded hour.
'''

if __name__ == "__main__":
    sys.exit(render("docs/hypodrafts/G1.md", TEXT))
