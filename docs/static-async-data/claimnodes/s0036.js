__STORE.put("claimnodes","s0036",{"X-CC-STSERIES":{"k":"script","l":"stations_series.py","d":"For each CTD cast, splits its depth range into quarters, keeps the shallowest as the surface and the deepest as the bed, and writes each variable's median per station and month, taken separately for each variable, to docs/data/areas/stations_series.bin."},"X-CONVEYANCE":{"k":"script","l":"conveyance.py","d":"Measures alignment length by typologi instead of quoting it."},"X-CS-CURRENTS":{"k":"script","l":"currents.py","d":"Fetches the Open-Meteo current field and the ERA5 wind, computes retention per point, the wind index, transport by rain band and lag, and the rain climate over the whole weather record; writes CURRENTS.md."},"X-CS-SOLUTIONS":{"k":"script","l":"solutions.py","d":"Reads Københavns Kommune's catchment and infiltration layers, the cloudburst lengths and the national overflow register and layer; writes SOLUTIONS.md."},"X-CYCLES":{"k":"script","l":"cycles.py","d":"Fixed effects in a moving window at each station; sun, moon, tide, temperature."},"X-DEPTHCLOCK":{"k":"script","l":"depth_clock.py","d":"Places CTD oxygen in time with a supplied, a borrowed or no clock, and checks the borrowed clock against supplied times where a cast has both."},"X-DETECT":{"k":"script","l":"detectable.py","d":"Synthetic recovery on the real schedule."},"X-DQ-CDSE":{"k":"script","l":"fetch_satellite.py: · Copernicus Data Space","d":"`scripts/fetch_satellite.py` reads Copernicus Data Space client credentials from files in the home directory, outside the repository, and fetches with them. The files were present on 2026-09-11 (their content was not read), and `data/raw/satellite` holds what it fetched."},"X-DQ-CMEMS":{"k":"script","l":"fetch_cmems.py: · Copernicus Marine","d":"`scripts/fetch_cmems.py` reads Copernicus Marine credentials from the home directory, outside the repository. They were present on 2026-09-11 (not read), and `data/raw/cmems` holds what it fetched."},"X-DQ-DEPTHPNG":{"k":"script","l":"floodmaps.py: · the depth raster","d":"`scripts/floodmaps.py` writes each sheet's depth raster as RGBA: where a band was read, that band's legend colour at full opacity; elsewhere transparent black. The comment above it says the band index is in the red channel; the code writes the colour."},"X-DQ-EXPORT":{"k":"script","l":"export_flood.py","d":"`scripts/export_flood.py` copies each placed sheet's depth raster from `data/derived/floodmaps`, writes beside it a world file (the pixel size and the centre of the top-left pixel) and a `WGS 84` projection file, records each sheet's placement method and standard error in `manifest.json`, and writes the README from the manifest."},"X-DQ-GITIGNORE":{"k":"script","l":".gitignore: · the drop-in directory","d":"The repository's `.gitignore` ignores `data/dropin/*` except its README, so a dropped file is not committed."},"X-DQ-ODACLIENT":{"k":"script","l":"oda_client.py","d":"`scripts/oda_client.py` logs in to ODA with an email address (`Login_DoLogin`) and extracts topics over SOAP from the portal's `Services.asmx`."},"X-DQ-QUEUE":{"k":"script","l":"fetch_queue.py · classify(), spatial_kind()","d":"`scripts/fetch_queue.py` reads the register files in its `FILES` list, `data_sources.json`, `data_sources_2.json` and `data_sources_3.json`. It puts each entry in a tier by keywords in its access text, in order: an entry recorded as on disk, a Copernicus Marine or Copernicus Data Space product, an ODA topic or a mention of Dataforsyningen is held; words for closed or unverified access make it blocked; words for a registration, account, login or token make it account; words for open or key-free access make it open; an entry matching none is blocked. It reads each entry's spatial and aggregation text for position or region keywords, lists as unlocks the hypotheses an entry names that no entry in an easier tier names, names the credentials it counts as held in `HELD` (the ODA login and the Dataforsyningen token), and writes `data/derived/fetch_queue.json` and DATA_QUEUE.md."},"X-DQ-TERRAIN":{"k":"script","l":"terraincheck.py: · the token","d":"`scripts/terraincheck.py` reads a Dataforsyningen token from a file in the home directory, outside the repository, and fetches the national elevation model's terrain over WCS. The file was present on 2026-09-11; its content was not read."},"X-DQ-UNLOCK":{"k":"script","l":"unlock.py","d":"`scripts/unlock.py` defines the drop-in slots, each a file name and shape under `data/dropin/` with the scripts that read it: only the CTD visit-time slot has one, `scripts/depth_clock.py`. Run without arguments it checks the shape of each dropped file (a delimited header, a line that parses as JSON, or TIFF bytes), runs the scripts that read a present slot, and rewrites IF_YOU_HAVE_THE_DATA.md; it opens no network connection. Its gated list is every entry of the register files the fetch queue reads that `classify()` does not put in the open tier."},"X-DS-COMMON":{"k":"script","l":"common.py: axis_order_ok","d":"Detects whether EPSG:4326 coordinates arrived as [lon, lat] or swapped, since WFS `1.0.0` and `1.1.0` disagree."},"X-DS-ENUMS":{"k":"script","l":"enums.py","d":"Counts every distinct value of every column of kemi and ctd."},"X-DS-FACTS":{"k":"script","l":"ds_facts.py","d":"Counts the traps over every row of every ODA extract."},"X-DS-FETCHWFS":{"k":"script","l":"fetch_wfs.py","d":"Fetches each layer anonymously and clips national layers by komm_navn, or by bounding box where a layer has no such column."},"X-DS-FLOODMAPS":{"k":"script","l":"floodmaps.py","d":"Renders the sheets, reads each scale bar from the text layer, checks every sheet's legend labels, masks the legend, and separates the palest band by its blue cast."},"X-DS-OBS":{"k":"script","l":"observations.py and viz/log.html","d":"The field logger and its checker: depth classes, drain states including surcharging, stored in data/manual/observations.geojson; the viewer build only copies the file out."},"X-DS-PAGE":{"k":"script","l":"data_sources.py","d":"Writes this page; loads every figure through live_json or reads it from a pinned document."},"X-DS-PLANFETCH":{"k":"script","l":"fetch_plan_projects.py","d":"Parses each plan page into JSON and keeps each stated quantity with the sentence it came from."},"X-DS-PROBE":{"k":"script","l":"ds_probe.py","d":"Asks each service once, anonymously, and stores the answer with the date."},"X-DS-REGISTRY":{"k":"script","l":"build_registry.py","d":"Recovers klima_id links from the plan pages' text, restricted to ids that exist in the geometry, and labels the result reconstructed, not official."},"X-DS-SCRIPTS":{"k":"script","l":"scripts/: imports","d":"No script imports GDAL or geopandas (a search of scripts/ on 2026-09-11)."},"X-DS-SPILDEVAND":{"k":"script","l":"fetch_spildevand.py","d":"Fetches the public spildevandsdata outfall layers anonymously and counts their features."},"X-DS-UNLOCK":{"k":"script","l":"unlock.py","d":"Holds the drop-in slots for data this project cannot reach, LER among them."},"X-DS-VIZ":{"k":"script","l":"viz/*.html","d":"The viewers load MapLibre from cdnjs."},"X-DZ-CONTIG":{"k":"script","l":"partition_contiguous.py","d":"contiguous_null grows random connected partitions by breadth-first accretion from random seeds, with group sizes matched to the official water bodies; main averages the adjusted Rand index over every pair of them for each graph and writes partition_contiguous.json."},"X-DZ-MATCHUP":{"k":"script","l":"matchup.py","d":"Pairs each light cast with the satellite pixel window over it on the same day. That matched casts are clearer than the rest in the same months is written into its docstring and its output's warning as text, with two medians, from its first run; neither median is computed or stored as a field when it runs."},"X-EA-BRIEF":{"k":"script","l":"agent_brief.py: seasonality","d":"Classifies every station of the monthly series by the brief's convention, pooled over the nine variables and on near-bed oxygen alone, counts the raw-R rule without a minimum, and writes data/derived/agent_brief.json."},"X-EA-EXPERIMENTS":{"k":"script","l":"experiments.py: the register X","d":"Each entry carries what it bears on, its kind and scale, and paragraphs for why, manipulate, control, measure, decide and a note; every entry has a control and a decide paragraph."},"X-EA-GLOSSARY":{"k":"script","l":"hypotheses.py: the glossary entry for fedtemøg","d":"The site's glossary, in scripts/hypotheses.py, gives fedtemøg as not a scientific term, and not measured by anything."},"X-FLOODGAP":{"k":"script","l":"floodgap.py","d":"Flood path against the cloudburst plan."},"X-FR-REPORT":{"k":"script","l":"report.py","d":"Renders the register page; splits each joined map value into its ids and looks each up among the ids the plan cites; renders a curated note only under a registered claim."},"X-FR-SEABED":{"k":"script","l":"seabed.py","d":"Reads the wave model's results and the rain climate; finds the months with most dead-bed hours and with most overflow-scale rain, refusing the page unless each forms a single run of the calendar with no tie at its edge; computes the illustration's arithmetic from its stated values."},"X-GC-AREAS-HZ":{"k":"script","l":"areas.py · the hazardous-substance section","d":"Its comment records that assigning the national layer's points to the nearest marine water body gave water bodies a hazardous-substance observation count made of lake and river points; the records now carry no marine hazardous-substance observation and report the gap."},"X-GC-CONSTRUCTED":{"k":"script","l":"constructed.py · the register's rows","d":"Reads each entry's result from a stored file - the water-body records, the partition subspace, the light record, the flood manifest, the architecture layers - or marks it as a claim resting on one checked on another page."}});
